# Project specification

A project specification is the versioned contract between intent and execution. It declares identity, material category, input structures, workflow stages, scientific parameters, resource classes, validation policy and output expectations.

Production specifications are schema-validated, reject unknown fields where practical, separate portable science from site configuration, and never contain credentials. Defaults are explicit and changes are reviewable.

