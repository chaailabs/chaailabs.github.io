# Provenance

Every scientific artifact should record:

- project and stage identifier;
- input hashes and parent artifacts;
- software/model version;
- configuration and physical parameters;
- scheduler job identifier and execution environment;
- timestamps and completion state;
- validation results and known limitations.

Figures must retain machine-readable source data. Model checkpoints must retain dataset identity, split strategy, training configuration, evaluation metrics, and intended domain.

